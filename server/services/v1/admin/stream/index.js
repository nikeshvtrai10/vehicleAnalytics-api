const listStream = require("./list-stream");
const createStream = require("./create-stream");
const getStream = require("./get-stream");
const deleteStream = require("./delete-stream");
const updateStream = require("./update-stream");

module.exports = {
  listStream,
  createStream,
  getStream,
  deleteStream,
  updateStream,
};
